{
  "type": "object",
  "properties": {
    "file_id": {
      "type": "number",
      "required": true,
      "title": "File ID",
      "description": "The ID of the uploaded file to attach"
    },
    "ref_id": {
      "type": "number",
      "required": true,
      "title": "Object ID",
      "description": "The ID of the object to attach the file to (item_id, task_id, etc.)"
    }
  }
}
